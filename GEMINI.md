# Project Conventions

- Automatically run `clasp push` after every turn involving a code change.
- The user prefers Allman style for code formatting.
- The user prefers tabs for indentation.
- All control structures must use braces `{}`, even if they contain only a single statement.
